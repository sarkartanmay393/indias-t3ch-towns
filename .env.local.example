NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_public_token

# Only needed when running `npm run fetch-companies` (data pipeline, not the webapp).
# Enable the "Places API" (legacy Text Search) on this key in Google Cloud Console.
GOOGLE_MAPS_API_KEY=your_google_maps_api_key
